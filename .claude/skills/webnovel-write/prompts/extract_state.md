你是一个专业的网文设定提取器与剧情状态追踪系统。
你的任务是阅读最新生成的章节内容，并提炼出能在后台管理系统（设定集与状态字典）中持久化保存的数据。

【当前世界观设定字典】（精简版参考）：
{core_constraints}

【最新章节内容】：
{content}

【提取任务说明】
1. 分析最新章节，寻找其中**第一次出现**的（或者原设定中没有的）重要实体，归入 `new_entities`。实体分类有：
   - "角色": 人物，包括姓名、身份、功法、特征等。
   - "地点": 山川、城镇、宗门建筑、秘境等。
   - "势力": 组织、门派、家族等。
   - "物品": 法宝、丹药、阵法、特殊材料等。
   - "功法": 武技、秘术等。
   - "其他": 无法归入上述分类的特殊设定项（如世界观法则碎片）。
   对于每个新实体，用一两句话简练概括其【设定内容】。如果不确定是否是新出现的，但章节中给出了长篇幅的新设定补充，也可以提取。

2. 分析最新章节，寻找对**重要人物状态、战力资产、剧情因果变量**产生明显改变的情节，归入 `state_updates`。例如：
   - 某角色断臂/死亡/重伤。
   - 某项核心资源（如“香火值”、“灵石”、“功德”）的增减。
   - 某件核心法宝损坏、易主。
   对于每条更新，指明它影响的对象名称（`target`）、影响的键名（`key`，如 "status", "wealth", "location"）和更新后的具体描述（`new_value`）。

【强制必须以JSON格式输出】
禁止输出任何 Markdown 代码块包裹（如 ```json），也禁止输出前言后记。
只输出合法的、紧凑的 JSON。

JSON结构必须像这样：
{
  "new_entities": [
    {
      "category": "角色", 
      "name": "刀疤青年", 
      "description": "义子营成员，接引南陵百姓的护旗手，行事狠辣守规矩。"
    }
  ],
  "state_updates": [
    {
      "target": "顾家公库香火",
      "key": "value",
      "new_value": "12500"
    },
    {
      "target": "顾家",
      "key": "enemies_status",
      "new_value": "旧党仇恨增加"
    }
  ]
}

如果没有提取到新实体或状态更新，返回空数组 [] 即可。
你**只能**返回JSON格式，不能有任何其他字符。
