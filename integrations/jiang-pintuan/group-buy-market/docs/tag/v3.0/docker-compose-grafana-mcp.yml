# Grafana MCP server https://github.com/grafana/mcp-grafana
version: '3.9'
# 鎵ц鑴氭湰锛沝ocker-compose -f docker-compose-grafana-mcp.yml up -d
services:
  # 鐩戞帶鏈嶅姟 mcp https://github.com/grafana/mcp-grafana
  grafana-mcp:
    image: mcp/grafana
    container_name: grafana-mcp
    ports:
      - "8000:8000"
    environment:
      - GRAFANA_URL=http://grafana:4000
      - GRAFANA_API_KEY=${GRAFANA_API_KEY:-replace-me}
    restart: unless-stopped
    networks:
      - my-network

networks:
  my-network:
    driver: bridge
